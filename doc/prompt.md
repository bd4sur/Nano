# 任务指令

## 闪念胶囊

用户输入：$user_input

现在时间：$current_date_time
请你从用户输入中提取时间、地点、人物、事项信息，按照以下格式，输出结构化的JSON。
你需要从用户提到的“明天”“昨天”等相对时间表述中，根据现在时间，换算成实际的日期和时间，严格按照样例格式输出。
如果用户输入中没有时间信息，则时间设定为现在时间的1个小时后。
输出仅含有JSON，输出仅含有JSON，不要输出任何额外字符。
格式样例：

{
    "time": "YYYY-MM-DD hh:mm",
    "location": "事项涉及的地点",
    "people": "事项涉及的人物",
    "note": "待办事项内容，不可遗漏关键信息。"
}

用户输入：$user_input

