{
    "train_batch_size": 2,
    "train_micro_batch_size_per_gpu": 1,
    "fp16": {
        "enabled": false
    },

    "optimizer": {
        "type": "AdamW"
    },

    "zero_optimization": {
        "stage": 3,
        "offload_optimizer": {
            "device": "cpu"
        },
        "offload_param": {
            "device": "cpu"
        }
    }
}
